(function(){"use strict";function switchManager(name){document.querySelectorAll("[data-admin-view]").forEach(button=>button.classList.toggle("active",button.dataset.adminView===name));document.querySelectorAll(".admin-manager").forEach(manager=>manager.classList.toggle("active",manager.id===`${name}Manager`));}document.querySelectorAll("[data-admin-view]").forEach(button=>button.addEventListener("click",()=>switchManager(button.dataset.adminView)));const select=document.querySelector("#eventPlace");if(select&&window.SAEROM_DATA){select.replaceChildren(new Option("장소를 선택하세요",""));[...window.SAEROM_DATA.places].sort((a,b)=>a.floor-b.floor||String(a.room).localeCompare(String(b.room),"ko",{numeric:true})).forEach(place=>select.add(new Option(`${place.floor}층 ${place.room}호 · ${place.name}`,place.id)));}}());
